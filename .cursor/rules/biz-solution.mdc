---
description: "业务知识库驱动的设计方案产出。用户提到设计方案、需求分析、页面设计、知识库时自动激活。4 阶段工作流：P1 需求理解 → P2 页面清单 → P3 逐页设计 → P4 整体评审。"
globs: "业务知识库/**/*.md, 业务知识库/manifest.json, 业务知识库/设计方案汇报模板.html, .biz-session.json"
alwaysApply: false
---

# biz-solution — 设计方案产出

当用户要求产出设计方案、分析需求或设计页面时，遵循以下工作流。

## 模板结构（双 Tab）

模板包含两个视图 Tab：
- **汇报 Tab**：领导视角。决策摘要(slot-summary) + 项目概览(A-D) + 设计规划(E-F) + 验证(H) + 命中与采用(I-summary)
- **工作台 Tab**：设计师视角。无限画布(pan/zoom) + 页面设计稿(G) + 跨域参考候选池(I-gallery)

## 启动协议

1. 读取 `业务知识库/manifest.json`（知识路由索引）
2. 读取 `业务知识库/设计方案汇报模板.html`（双 Tab HTML 模板）
3. 检查 `业务知识库/.biz-session.json`（会话状态，存在则继续）
4. 知识路由：
   - 第一遍：`keywords` + `match` 精确命中 1-3 篇文档
   - 第二遍：扫描 `pages[]` 做跨域相似匹配 + `flowArchetype` 做流程同构匹配
   - 按 `sectionSchema` 分阶段读章节，不要一次读全文
5. 复制模板到 `业务知识库/output/{简称}-report.html`

## 来源标注

每条内容必须标注：知识库（精确命中）/ 跨域参考（相似页面）/ 推理（知识库无覆盖）

## 槽位操作

- 定位 `id="slot-X"` 替换 placeholder
- **slot-summary**（汇报 Tab）= P4 填充决策摘要（KV 表格：结论/理由/复用边界/产物）
- **I-gallery**（`id="slot-i-gallery"`，工作台 Tab）= 只追加不替换
- **I-summary**（`id="slot-i-summary"`，汇报 Tab）= P4 定稿时一次性写入

## 四阶段工作流

### P1 需求理解 → Slot A/B/C/D（汇报 Tab）

读取命中文档的「项目目标」「目标用户特征」「数据指标」章节。
- A 需求背景 / B 产品目标 / C 目标用户 / D 关键指标
- I-gallery 追加跨域截图（工作台 Tab）
- 知识库预检：扫描决策点覆盖度，写入 `slot-gap-check`
- **暂停**：请用户确认 Part 1

### P2 页面清单 → Slot E/F（汇报 Tab）

读取「页面识别总表」「黄金流程」章节。
- E 设计流程（主链路 + 分支 + 流程同构分析）
- F 页面清单表格
- crossRefs 标记 adopted: true/false
- **暂停**：请用户确认页面清单

### P3 逐页设计 → Slot G（工作台 Tab）

对 inventory 所有页面一次性设计 iOS 375×812 pt UI。
推荐 matrix 布局（每页一列：node-card → phone-frame → evidence-shot）。
引用 adopted=true 的跨域截图作参考。
- **暂停**：请用户确认设计

### P4 整体评审 → Slot summary + H + I-summary（汇报 Tab）

- summary 决策摘要（KV 表格，结论先行）
- H 四维自检（流程完整 / 规则一致 / UI 合规 / 信息不足）
- I-summary 定稿（知识库/跨域/推理 汇总 + 知识库建议 + 矛盾检测）
- **暂停**：请用户最终确认

## 产出

最终产物：`业务知识库/output/{简称}-report.html`（双 Tab，全部槽位填满）
