# Folder: 业务知识库/examples
> L2 | 父级: ../CLAUDE.md

> 验证案例存档。这里是历史测试产物，**不是知识文档**。Agent 做设计方案时不应读取本目录。

## 成员清单

- `风控验证案例/`: 风控代还刷脸鉴权验证案例（report + canvas HTML），展示 Agent 调用知识库的效果。

**注意**：本目录不在 manifest.json 索引范围内，AI 不应将此处内容作为业务知识来源。

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
